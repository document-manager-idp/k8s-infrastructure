apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: ghcr.io/document-manager-idp/auth-service:83c9b4fd8d6a76cc4a05a486c4489aecfff3585b
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: HOSTNAME
              value: localhost
            - name: PORT
              value: "3000"
            - name: REDIRECT_URL
              value: http://localhost
            - name: COGNITO_SECRET
              valueFrom:
                secretKeyRef:
                  name: cognito-secret
                  key: COGNITO_SECRET
